# Local dev
# DATABASE_URL="postgresql://postgres:postgres@localhost:5433/foodapp?schema=public"

# Render (external — use from local machine)
# DATABASE_URL="postgresql://traking_ai8h_user:<password>@dpg-d85fd9vavr4c73fghvn0-a.singapore-postgres.render.com/traking_ai8h"

# Render (internal — use from Render-deployed services)
# DATABASE_URL="postgresql://traking_ai8h_user:<password>@dpg-d85fd9vavr4c73fghvn0-a/traking_ai8h"

DATABASE_URL="postgresql://traking_ai8h_user:<password>@dpg-d85fd9vavr4c73fghvn0-a.singapore-postgres.render.com/traking_ai8h"
PORT=3001

# Required: must be at least 32 characters. Generate with: openssl rand -base64 32
JWT_SECRET=your-very-long-random-secret-at-least-32-chars

# Comma-separated list of allowed CORS origins
ALLOWED_ORIGINS=http://localhost:3000
